export class AbrirCerrar {

    idRestaurante : string;
    abierto: boolean;

    

    constructor(idRestaurante: string, abierto: boolean)
        {
           this.idRestaurante = idRestaurante;
           this.abierto = abierto;
     
    }

}
